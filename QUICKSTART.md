# 🚀 Быстрый старт TwitRec

## Установка за 3 шага

### 1. Установить зависимости
```bash
pip install -r requirements.txt
pip install streamlink  # или используйте apt/brew
```

### 2. Получить Twitch API ключи
1. Перейдите на https://dev.twitch.tv/console/apps
2. Создайте новое приложение
3. Скопируйте Client ID и Client Secret

### 3. Запустить приложение
```bash
python main.py
```

При первом запуске введите ваши API ключи.

## Быстрая запись стрима

1. Выберите `1` - Начать запись стрима
2. Введите имя стримера (например: `shroud`)
3. Выберите качество (`1` для лучшего)
4. Готово! Файл сохранится в папке `recordings/`

## Фоновый режим

Для автоматической записи:

1. Выберите `7` - Фоновый режим
2. Добавьте стримеров для отслеживания
3. Запустите мониторинг
4. Приложение будет записывать стримы автоматически!

## Где найти записи?

Все записи сохраняются в папке `recordings/`
Логи находятся в папке `logs/`

## Нужна помощь?

Смотрите полную документацию в [README.md](README.md)
